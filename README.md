# HollApp

To run the application

```bash
# Install the dependencies
bower install

# Install simple web server
npm install -g serve

npm install -g http-server

# Run
http-server
or
serve
`
"``
Starting up http-server, serving ./
Available on:
  http://127.0.0.1:8081
  http://192.168.137.224:8081
Hit CTRL-C to stop the server
"
